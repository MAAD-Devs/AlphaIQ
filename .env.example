# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# Supabase direct connection string (used by dbmate for migrations).
# Find it in Supabase dashboard: Settings > Database > Connection string
# Use the "Direct connection" URI (not the pooler).
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.auiwbscrnzsnffrfbdxt.supabase.co:5432/postgres

# Set to TEST to enable verbose (DEBUG) logging locally. Omit in production.
# APP_ENV=TEST
